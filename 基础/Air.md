# Air

## 基础介绍


### air
```yaml
air:
    -d:
    init: # 初始化
```

#### .air.toml
```yaml
air.toml:
    build:
        bin: # 运行命令
        cmd: # 构建命令
        delay:
        exclude_dir:
        exclude_file:
        exclude_regex:
        exclude_unchanged:
        full_bin:
        include_dir:
        include_ext:
        include_file:
        log:
        stop_on_error:
    color:
        app:
        build:
    log:
        time:
    misc:
    root: # 项目根目录
    screen:
        clear_on_rebuild:
    testdata_dir:
    tmp_dir: # 临时文件目录
```


## 核心内容
